export const BRANCH_NAME = 'cross-repo-sync/update'

export const PR_LABEL = 'cross-repo-sync'

export const PR_MARKER = '<!-- cross-repo-sync-marker: do-not-remove -->'

export const DEFAULT_CONFIG_PATH = 'sync-config.yml'

export const DEFAULT_PR_TITLE_PREFIX = 'chore: sync files from'

export const DEFAULT_COMMIT_MESSAGE_PREFIX = 'chore: sync files from'
